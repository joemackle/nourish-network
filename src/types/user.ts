/*import { User } from '@prisma/client';

type UserSummary = Pick<User, 'id' | 'username' | 'group'>;

export interface UserSummaryAfterSetUp {
  id: string;
  username: string;
  group: string;
  //name: string;
  //profilePhoto: string | null;
}

interface UserAfterSetUp extends User {
  username: string;
  name: string;
}*/
